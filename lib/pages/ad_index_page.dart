import 'package:flutter/material.dart';

class AdsIndexPage extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        centerTitle: true,
        title: Text('ad_main'),
        automaticallyImplyLeading: false,
      ),
      body: Center(
        child: Text('ad_main'),
      ),
    );
  }
}
