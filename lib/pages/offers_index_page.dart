import 'package:flutter/material.dart';

class OffersIndexPage extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        centerTitle: true,
        title: Text('offer_main'),
        automaticallyImplyLeading: false,
      ),
      body: Center(
        child: Text('offer_main'),
      ),
    );
  }
}
